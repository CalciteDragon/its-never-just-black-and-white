{
  "name": "its-never-just-black-and-white",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "A minimalist momentum platformer. A rigid-body square with real corner collision, and a flip that inverts both the palette and gravity. TypeScript + Canvas, zero runtime dependencies.",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
