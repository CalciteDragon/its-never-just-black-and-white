{
  "name": "pixel-quest",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A pixel-art dungeon platformer with procedurally generated levels, local two-player co-op, and a daily challenge. TypeScript + Canvas, zero runtime dependencies.",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
